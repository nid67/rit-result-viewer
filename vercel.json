{
  "buildCommand": "echo 'Static site with serverless functions'",
  "outputDirectory": ".",
  "public": true,
  "framework": "other",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    }
  ]
}
